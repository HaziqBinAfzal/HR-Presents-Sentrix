Single-database migration configuration for Sentrix.

Use `flask db upgrade` during deployment before starting the web process.
Create future schema changes with `flask db migrate -m "description"` and review generated migrations before applying them.
